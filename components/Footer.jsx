import React from 'react'

const calculateTip = (amount) => {
    const total;

    switch(amount) {
        case amount > 10: 
            return total = 0.2;
    }
}

const value = calculateTip(20);
console.log(value)

export default Footer;
